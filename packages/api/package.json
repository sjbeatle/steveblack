{
  "name": "steveblack-api",
  "version": "1.0.0",
  "description": "Official API for steveblackonline.com",
  "main": "dist/app.js",
  "scripts": {
    "start": "node dist/app.js",
    "build": "rm -rf dist && tsc",
    "clean": "rm -rf package-lock.json dist node_modules",
    "clean-dist": "rm -rf dist",
    "clean:install:build": "npm run clean && npm i && npm run build",
    "dev": "nodemon src/app.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Steve Black <stevenjblack@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.6",
    "@types/node": "^14.0.13",
    "nodemon": "^2.0.4",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.5"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
